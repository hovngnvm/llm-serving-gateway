# ==============================================================================
# AI Serving & Security Gateway - Unified Platform Dependencies
# ==============================================================================

# --- 1. Web Framework, Async API & Gateway Core ---
fastapi>=0.110.0,<0.120.0
uvicorn[standard]>=0.29.0
starlette<0.49.0
pydantic>=2.7.0
pydantic-settings>=2.2.0
httpx>=0.27.0
python-dotenv>=1.0.1

# --- 2. LLM Client & OpenAI Compatibility ---
openai>=1.30.0

# --- 3. Vector Semantic Caching (Redis) ---
redis>=5.0.4
numpy>=1.26.0

# --- 4. Privacy & PII Redaction Engine (Microsoft Presidio) ---
presidio-analyzer>=2.2.354
presidio-anonymizer>=2.2.354
spacy>=3.7.4
pillow>=10.3.0
pytesseract>=0.3.10

# --- 5. JSON Auto-Repair & Schema Parsing ---
json-repair>=0.25.0

# --- 6. Cloud Audit Logging (PostgreSQL SSL/TLS) ---
asyncpg>=0.29.0

# --- 7. Telemetry & Metrics (Prometheus) ---
prometheus-fastapi-instrumentator>=7.0.0,<8.0.0
prometheus-client>=0.20.0

# --- 8. MLOps, SFT & QLoRA Fine-Tuning Pipeline ---
torch>=2.2.0
transformers>=4.40.0
peft>=0.10.0
datasets>=2.19.0
pyyaml>=6.0.1
accelerate>=0.30.0
bitsandbytes>=0.43.0
tqdm>=4.66.0

# --- 9. Testing & Quality Assurance ---
pytest>=8.2.0
pytest-asyncio>=0.23.6
