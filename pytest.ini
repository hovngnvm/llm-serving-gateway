[pytest]
testpaths = tests
pythonpath = .
asyncio_mode = auto
markers =
    live: marks tests requiring live network services (deselect with '-m "not live"')
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
